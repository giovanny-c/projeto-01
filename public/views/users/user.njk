{% extends "views/layout.njk" %}


{% block head %}

    <link rel="stylesheet" href="/styles/users/user.css">
    <title>Usuário - {{user.name}} - Castanharo Telemarketing</title>

{% endblock %}

{% block content %}


    <div class="wrap">

        <div class="worker">
            <p>Usuário: {{user.name}} </p>
            <p>Email: {{user.email}}</p>
            
            {% if user.worker_id %}

                <p>funcionário: {{user.worker.name}}</p>
                
            {% endif %}

        </div>

    
       {% if admin_user.id != user.id and admin_user.admin and user.admin %}
            
            
       {% else %}
            

                <div class="form-update">

                        <form name="form-update" class="form-update" action="/usuarios/{{user.id}}/editar" method="GET" enctype="multipart/form-data">
                    

                        <button class="update"  type="submit">
                            Editar
                        </button>
                        </form>
                </div>

                {# <div>
                    <form name="form-delete" class="form-delete" action="/usuarios/{{user.id}}/deletar?_method=DELETE" method="POST" enctype="multipart/form-data">
                    

                        <button class="cancel"  type="submit">
                            Deletar
                        </button>
                
                    </form>
                </div> #}

            
              

            
       {% endif %}
        </div>  

    {# <a href="/donations/receipt/{{donation.id}}" class="button" target="_blank">Gerar Recibo</a> #}

{% endblock %}  


